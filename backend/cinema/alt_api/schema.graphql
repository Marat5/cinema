schema {
  query: Query
  mutation: Mutation
}

type Director {
  id: ID!
  name: String!
}

type Query {
  directors: [Director!]
  director(directorId: ID!): Director!
}

type Mutation {
  addDirector(name: String!): Director
  deleteDirector(directorId: ID!): Boolean
}
